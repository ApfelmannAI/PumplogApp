services:
  discord-listener:
    image: python:3.12-slim
    working_dir: /app
    env_file:
      - .env
    volumes:
      - ./app:/app:ro
      - ./outbox:/data/outbox
    command: ["sh", "-c", "pip install --no-cache-dir -r requirements.txt && python listener.py"]
    restart: unless-stopped
